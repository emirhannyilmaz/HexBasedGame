#include "Hex.h"

Hex::Hex(Model* model, std::vector<Collider> colliders, glm::vec3 position, float rotationInDegrees, glm::vec3 rotationAxis, glm::vec3 scale, std::string name) : Entity(model, colliders, position, rotationInDegrees, rotationAxis, scale, name) {
	
}